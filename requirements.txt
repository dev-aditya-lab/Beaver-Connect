# ─────────────────────────────────────────────────────────────────────────────
# Beaver Connect — Python Dependencies
# BeaverX Pvt. Ltd.
#
# Install: pip install -r requirements.txt
# Python : 3.12+
# ─────────────────────────────────────────────────────────────────────────────

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.115.5            # Modern async web framework; powers all REST endpoints
uvicorn[standard]==0.32.1   # ASGI server that runs FastAPI; [standard] adds websocket support

# ── Serial Port / Hardware Detection ─────────────────────────────────────────
pyserial==3.5               # USB serial port enumeration and board identification

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic==2.10.1            # Request/response schema validation (FastAPI dependency)

# ── HTTP Client (optional — for future OTA / cloud features) ─────────────────
httpx==0.27.2               # Async HTTP client; useful for outbound requests later

# ── Packaging ─────────────────────────────────────────────────────────────────
pyinstaller==6.11.1         # Bundle into a single Windows .exe for distribution
